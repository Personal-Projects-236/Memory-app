import Header from "../components/organism/Header.jsx";

// NOTE: below the title the page will be split between the left and right
// NOTE: the left will be a react hook form, this will be for all inputted data
// NOTE: the right will be displaying all the memories from the database

// TODO: create two components for the left and right hand side
// TODO: surrounding it with a div using the flex box model
// TODO: the left component will have a width of 30% on desktop and 100% on mobile
// TODO: the right component will have a width of 70% on desktop and 100% on mobile

const App = () => {
	return <Header />;
};

export default App;
